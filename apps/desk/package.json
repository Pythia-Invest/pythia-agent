{
  "name": "@pythia/desk",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "next build --webpack",
    "check": "next typegen && tsc --noEmit --project tsconfig.json",
    "dev": "next dev --webpack",
    "test": "pnpm run test:unit",
    "test:unit": "tsc --noEmit --project tsconfig.test.json && vitest run --passWithNoTests"
  },
  "dependencies": {
    "@pythia/ui": "workspace:*",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
