mcp_servers:
  basic-memory:
    url: "${PYTHIA_BASIC_MEMORY_MCP_URL}"
    enabled: true
    timeout: 30
    connect_timeout: 10
    supports_parallel_tool_calls: false
    tools:
      resources: true
      prompts: true
