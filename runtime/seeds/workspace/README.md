# Investment workspace

This directory belongs to the investor after first installation. Pythia updates
never overwrite it. `portfolio/` and `cases/` hold working material;
`scratch/` is disposable. Put durable, indexed research in the configured
Basic Memory Markdown project rather than creating another knowledge store here.
